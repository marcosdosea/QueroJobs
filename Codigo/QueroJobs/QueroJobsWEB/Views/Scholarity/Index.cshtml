@model IEnumerable<QueroJobsWEB.Models.ScholarityModel>

@{
    ViewData["Title"] = "QueroJobs - Escolaridade";
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-action="Create">Criar novo escolaridade</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ScholarityName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ScholarityName)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Editar</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Deletar</a>
                </td>
            </tr>
        }
    </tbody>
</table>